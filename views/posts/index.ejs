<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #4CAF50;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            background-color: white;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin: 10px 0;
            padding: 20px;
        }
        h2 {
            margin-bottom: 10px;
            color: #333;
        }
        p {
            font-size: 1em;
            line-height: 1.5;
            color: #555;
        }
        span {
            font-size: 0.9em;
            color: #777;
        }
        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }
        button a {
            color: white;
            text-decoration: none;
        }
        button:hover {
            background-color: #45a049;
        }
        a {
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        /* Stil za dugme za novi post */
        .new-post-btn {
            display: block;
            width: fit-content;
            margin: 20px auto;
            background-color: #2196F3;
        }
        .new-post-btn:hover {
            background-color: #0b7dda;
        }
    </style>
</head>
<body>
    <h1>All posts on our media</h1>
    <ul style="list-style: none;">
        <%for(let post of posts){%>
            <li style="text-decoration: none;">
                <h2><%=post.title%></h2>
                <p><%=post.text%></p>
                <span><%=post.author%></span><br>
                <span>
                    <button class="likeBtn"><%=post.likes%></button>👍
                </span>
                <span>Datum:<%=post.createdAt%></span>
                <button><a href="posts/<%=post.id%>">Detaljnije...</a></button>
            </li>   
        <%}%>
    </ul>
    <button><a href="/posts/new">Napravi novi post</a></button>
    <script>
        document.querySelectorAll('.likeBtn').forEach(btn => {
        btn.addEventListener('click', () => {
        let value = parseInt(btn.innerText);
        value++;
        btn.innerText = value;
        });
});
    </script>
</body>
</html>